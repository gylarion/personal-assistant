# Персональний помічник (Personal Assistant)

**Персональний помічник** — це консольна програма, створена для зручного зберігання та обробки контактної інформації й нотаток.  
Вона працює у вигляді CLI (Command Line Interface), дозволяючи користувачу керувати контактами та нагадуваннями прямо з терміналу.

## 🔍 Призначення

Програма розроблена для особистого використання й автоматизації повсякденних завдань, зокрема:

- ведення адресної книги з телефонами, email, адресами та днями народження
- зберігання нотаток і тегів
- нагадування про майбутні дні народження
- пошук і редагування даних
- взаємодія через прості текстові команди

---

## ⚙️ Основні можливості

### 📇 Робота з контактами:
- `add [name] [phone]` — додати новий контакт або додати телефон до наявного
- `change [name] [old_phone] [new_phone]` — змінити номер телефону
- `phone [name]` — вивести всі телефони контакту
- `add-birthday [name] [DD.MM.YYYY]` — додати дату народження
- `show-birthday [name]` — показати день народження
- `birthdays` — вивести список днів народження на наступні 7 днів
- `all` — показати всі контакти

### 🗒️ Робота з нотатками *(опціонально, якщо реалізовано)*:
- `add-note [name] [text] [#tag]` — додати нотатку до контакту
- `edit-note [name] [index] [new_text]` — змінити текст нотатки
- `delete-note [name] [index]` — видалити нотатку
- `search-notes [text or #tag]` — пошук по нотатках

### 🔚 Загальні команди:
- `hello` — привітання
- `help` — список доступних команд
- `exit` або `close` або `quit` — завершити роботу з ботом

---

## 🛠️ Як запустити

1. Переконайтесь, що у вас встановлений Python 3.10+
2. Встановіть залежності (якщо є):
```bash
pip install -r requirements.txt
```
3. Запустіть програму:
```bash
python main.py
```

---

## 💾 Збереження даних

Дані зберігаються автоматично у файл `addressbook.pkl` при завершенні роботи.  
Під час наступного запуску — дані будуть завантажені автоматично.

---

## 📂 Структура проєкту

```
main.py                  # Точка входу, головний CLI цикл
task1.py                 # Класи Field, Name, Phone, Record, AddressBook
task2.py                 # Обробники команд користувача
addressbook.pkl          # Файл збережених даних (генерується автоматично)
.gitignore
requirements.txt
README.md
LICENSE
```

---

## 📜 Ліцензія

Проєкт розповсюджується під [MIT License](LICENSE).

# Приклад інтеграції в main.py

    ...
    elif command == "add-note":
        # args вже містять все, крім самої команди
        print(add_note(args, book)) 
        
    elif command == "edit-note":
        print(edit_note(args, book))
        
    elif command == "delete-note":
        print(delete_note(args, book))
        
    elif command == "search-notes":
        print(search_notes(args, book))

    elif command == "notes-by-tag":
        print(sort_notes_by_tag(args, book)) # args буде порожнім

    # Команди вашого колеги
    elif command == "find":
        print(Contactss(args, book))

    elif command == "contacts":
        print(show_all_contacts(book))
    ...